#include<bits/c++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n,l,k;
        cin >> n >> k >> l;
        vector<int> d(n);
        for (auto &it : d) cin >> it;
        vector<int> res(n);
        
        // cout << "Yes";
    }
}